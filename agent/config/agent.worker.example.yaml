ROLE: worker
HOST: 10.130.36.21
PORT: 8899
LLM_URL: http://10.130.39.6:6000/v1
LLM_MODEL: Qwen3.6-27B

RESOURCE_POOL:
  # Must match the controller setting in a resource-pool-managed deployment.
  ENABLED: true

ADMIN:
  # Enables local Admin maintenance endpoints.
  # Set MEDFLOW_ADMIN_TOKEN in the process environment before enabling.
  ENABLED: true

MEMORY:
  BACKEND: memory
